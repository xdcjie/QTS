# Factors Rules

This module contains cross-sectional factor computations.

Rules:

- Factor outputs must be time-sliced and avoid look-ahead bias.
- Keep ranking/normalization semantics explicit.
- User-facing access should be exposed through Strategy SDK.
