# GC 15m VWAP round 04 matrix: exit/target controls after bad-regime gate.
#
# Research evidence only. report_2025_2026 is a holdout report/veto.

version: 1
workflow_id: vwap-factor-gc-15m-round04-exit-target-matrix
steps:
  - id: implementation
    kind: implementation_gate
    required_strategy: strategies.research.vwap_factor_research:VwapFactorResearchStrategy

  - id: matrix
    kind: backtest_matrix
    output_root: ../../../runs/research/vwap/gc-15m-long/round04-exit-target-prefilter
    summary_output: ../../../runs/research/vwap/gc-15m-long/validation/round04-exit-target-prefilter.json
    metrics: [total_return, sharpe_ratio, max_drawdown, total_trades, profit_factor]
    base_strategy_params:
      symbol: GC
      time_window: asia_20_02
      target_quantity: "4"
      min_volume_ratio: "1.3"
      pullback_touch_atr_below: "0.15"
      max_pullback_break_atr: "1.0"
      stop_atr_multiple: "1.0"
      target_r_multiple: "2.0"
    periods:
      - name: failure_2022_2024
        start: 2022-01-01
        end: 2025-01-01
      - name: report_2025_2026
        start: 2025-01-01
        end: 2026-04-10
    candidates:
      - name: base
        strategy_params:
          factor_filters: [session_sigma_range, mom120_aligned]
      - name: base_vwap_exit
        strategy_params:
          factor_filters: [session_sigma_range, mom120_aligned]
          exit_on_vwap_cross: true
      - name: bad_hard14_accept_075
        strategy_params:
          factor_filters: [session_sigma_range, mom120_aligned, vwap_acceptance_if_bad_regime]
          bad_regime_rule: hard14
          bad_regime_unready_policy: allow
          vwap_acceptance_min: "0.75"
      - name: bad_hard14_accept_075_vwap_exit
        strategy_params:
          factor_filters: [session_sigma_range, mom120_aligned, vwap_acceptance_if_bad_regime]
          bad_regime_rule: hard14
          bad_regime_unready_policy: allow
          vwap_acceptance_min: "0.75"
          exit_on_vwap_cross: true
      - name: bad_hard14_accept_075_target15
        strategy_params:
          factor_filters: [session_sigma_range, mom120_aligned, vwap_acceptance_if_bad_regime]
          bad_regime_rule: hard14
          bad_regime_unready_policy: allow
          vwap_acceptance_min: "0.75"
          target_r_multiple: "1.5"

  - id: report
    kind: research_report
    output_root: ../../../runs/research/vwap/gc-15m-long/reports
    output_path: vwap-factor-gc-15m-round04-exit-target-prefilter-report.md
